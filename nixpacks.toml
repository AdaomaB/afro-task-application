[phases.setup]
nixPkgs = ["nodejs_20"]
nixpacksVersion = "1.21.0"

[phases.install]
cmds = ["npm ci --only=production || npm install --only=production"]

[phases.build]
cmds = ["echo 'Build complete - Node.js backend ready'"]

[start]
cmd = "node server.js"
